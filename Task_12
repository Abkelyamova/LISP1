; Задача № 12 Вариант 1
; Определите функцию, заменяющую в исходном списке два подряд идущих одинаковых элемента одним.

(defun gulya (list) 
  (when list ((lambda (x y)
             (cons x  (gulya (if (eq x (car y)) (cdr y) y))))
           (car list) (cdr list))))
 
(print (gulya '(a a a b b n n n n)))
